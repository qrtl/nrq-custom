This module limits the create/write right to the users with the 'Contact Creation' group
(base.group_partner_manager).

As an exception, users are able to update the partners that link to themselves.
